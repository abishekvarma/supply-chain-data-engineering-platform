# Data

This directory documents the data layers, schemas, sample datasets, and data management conventions used by the platform.

Raw and production datasets are intentionally excluded from version control.
